# MyExam 아키텍처 문서

> **버전**: 1.0.0  
> **최종 수정**: 2024-12  
> **작성자**: 개발팀

---

## 목차

1. [개요](#1-개요)
2. [시스템 아키텍처](#2-시스템-아키텍처)
3. [프로젝트 구조](#3-프로젝트-구조)
4. [기술 스택](#4-기술-스택)
5. [API 설계](#5-api-설계)
6. [데이터베이스 설계](#6-데이터베이스-설계)
7. [인증 및 보안](#7-인증-및-보안)
8. [개발 환경 설정](#8-개발-환경-설정)

---

## 1. 개요

### 1.1 프로젝트 목적

MyExam은 거북스쿨의 **모의고사 분석 서비스**를 분리 운영하기 위한 마이크로서비스입니다.

### 1.2 분리 운영 배경

```
┌────────────────────────────────────────────────────────────────────────────┐
│                            분리 운영 아키텍처                                │
├────────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│                        프론트엔드                                           │
│              (turtleskool.com, admin.xxx)                                  │
│                            │                                               │
│         ┌──────────────────┼──────────────────┐                           │
│         │                  │                  │                           │
│         ▼                  ▼                  ▼                           │
│  ┌─────────────┐   ┌─────────────┐   ┌─────────────┐                     │
│  │ 메인 백엔드  │   │ 플래너 API  │   │ 모의지원 API │  ← MyExam          │
│  │   :4001     │   │   :4002     │   │   :4003     │                     │
│  │  /api/*     │   │ /planner/*  │   │  /exam/*    │                     │
│  └──────┬──────┘   └──────┬──────┘   └──────┬──────┘                     │
│         │                  │                  │                           │
│         └──────────────────┼──────────────────┘                           │
│                            ▼                                               │
│              ┌─────────────────────────┐                                  │
│              │     공유 PostgreSQL      │                                  │
│              │   (member_tb 등 공용)    │                                  │
│              └─────────────────────────┘                                  │
│                            ▲                                               │
│              ┌─────────────────────────┐                                  │
│              │    공유 패키지 (npm)     │                                  │
│              │  @geobuk/shared-types   │                                  │
│              │  @geobuk/common-utils   │                                  │
│              └─────────────────────────┘                                  │
│                                                                            │
└────────────────────────────────────────────────────────────────────────────┘
```

### 1.3 주요 기능

| 기능 | 설명 |
|------|------|
| 성적 입력 | 모의고사 원점수/표준점수 입력 (고1/고2/고3) |
| 성적 분석 | 과목별, 조합별, 반영비율별 성적 분석 |
| 대학 예측 | 입력된 성적 기반 대학 합격 가능성 예측 |
| 누적 분석 | 모의고사별 성적 추이 및 누적 분석 |
| 목표 대학 | 목표 대학 설정 및 등급컷 비교 |

---

## 2. 시스템 아키텍처

### 2.1 전체 구조

```
┌─────────────────────────────────────────────────────────────────┐
│                       MyExam 서비스                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────┐         ┌─────────────────────────────┐   │
│  │    Frontend     │  ←───→  │         Backend             │   │
│  │  localhost:3000 │   API   │      localhost:4003         │   │
│  │                 │         │                             │   │
│  │  - React 18     │         │  - NestJS 10                │   │
│  │  - Vite         │         │  - TypeORM                  │   │
│  │  - TanStack     │         │  - @geobuk/shared-*         │   │
│  │  - Tailwind CSS │         │  - Swagger                  │   │
│  └─────────────────┘         └──────────────┬──────────────┘   │
│                                             │                   │
└─────────────────────────────────────────────┼───────────────────┘
                                              │
                                              ▼
┌─────────────────────────────────────────────────────────────────┐
│                     공유 PostgreSQL                              │
│                   (geobukschool_dev)                            │
├─────────────────────────────────────────────────────────────────┤
│  member_tb │ ts_universities │ mockexam_*_tb │ ts_regular_*    │
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 통신 흐름

```
[사용자] → [Frontend :3000] → [Backend :4003] → [PostgreSQL]
                │                    │
                │                    └─→ [공유 패키지 @geobuk/*]
                │
                └─→ [Vite Proxy] → [Backend :4003]
```

---

## 3. 프로젝트 구조

### 3.1 전체 디렉토리 구조

```
MyExam/
│
├── 📂 frontend/                      # React + Vite 프론트엔드
│   ├── src/
│   │   ├── components/
│   │   │   ├── ui/                   # shadcn/ui 컴포넌트
│   │   │   ├── my-exam-header.tsx
│   │   │   ├── sub-sidebar-nav.tsx
│   │   │   ├── footer.tsx
│   │   │   └── scroll-to-top.tsx
│   │   │
│   │   ├── routes/                   # TanStack Router
│   │   │   ├── __root.tsx
│   │   │   ├── index.tsx
│   │   │   ├── auth/
│   │   │   │   ├── login.tsx
│   │   │   │   └── register.tsx
│   │   │   └── my-exam/
│   │   │       ├── route.tsx
│   │   │       ├── _layout.tsx
│   │   │       ├── _layout.score-input.lazy.tsx
│   │   │       ├── _layout.score-analysis.lazy.tsx
│   │   │       ├── _layout.prediction.lazy.tsx
│   │   │       ├── _layout.statistics.lazy.tsx
│   │   │       ├── _layout.wrong-answers.lazy.tsx
│   │   │       └── _layout.target-university.lazy.tsx
│   │   │
│   │   ├── lib/
│   │   │   └── utils.ts
│   │   │
│   │   ├── stores/                   # (추가 예정) 상태 관리
│   │   │   ├── client/               # Zustand 스토어
│   │   │   └── server/               # TanStack Query
│   │   │
│   │   ├── index.css
│   │   ├── main.tsx
│   │   └── routeTree.gen.ts
│   │
│   ├── public/
│   ├── package.json
│   ├── vite.config.ts
│   ├── tailwind.config.js
│   └── tsconfig.json
│
├── 📂 backend/                       # NestJS 백엔드 (예정)
│   ├── src/
│   │   ├── app.module.ts
│   │   ├── main.ts
│   │   │
│   │   ├── modules/
│   │   │   ├── auth/                 # 인증 모듈
│   │   │   │   ├── auth.module.ts
│   │   │   │   ├── auth.guard.ts
│   │   │   │   └── auth.service.ts
│   │   │   │
│   │   │   ├── score/                # 성적 입력/관리
│   │   │   │   ├── score.module.ts
│   │   │   │   ├── score.controller.ts
│   │   │   │   ├── score.service.ts
│   │   │   │   ├── dto/
│   │   │   │   │   ├── create-raw-score.dto.ts
│   │   │   │   │   ├── create-standard-score.dto.ts
│   │   │   │   │   └── score-response.dto.ts
│   │   │   │   └── entities/
│   │   │   │       ├── raw-score.entity.ts
│   │   │   │       └── standard-score.entity.ts
│   │   │   │
│   │   │   ├── analysis/             # 성적 분석
│   │   │   │   ├── analysis.module.ts
│   │   │   │   ├── analysis.controller.ts
│   │   │   │   └── analysis.service.ts
│   │   │   │
│   │   │   ├── prediction/           # 대학 예측
│   │   │   │   ├── prediction.module.ts
│   │   │   │   ├── prediction.controller.ts
│   │   │   │   └── prediction.service.ts
│   │   │   │
│   │   │   ├── university/           # 대학 정보
│   │   │   │   ├── university.module.ts
│   │   │   │   ├── university.controller.ts
│   │   │   │   └── university.service.ts
│   │   │   │
│   │   │   └── statistics/           # 누적 통계
│   │   │       ├── statistics.module.ts
│   │   │       ├── statistics.controller.ts
│   │   │       └── statistics.service.ts
│   │   │
│   │   ├── common/                   # 공통 모듈
│   │   │   ├── decorators/
│   │   │   │   └── current-user.decorator.ts
│   │   │   ├── filters/
│   │   │   │   └── http-exception.filter.ts
│   │   │   ├── guards/
│   │   │   │   └── jwt-auth.guard.ts
│   │   │   └── interceptors/
│   │   │       └── transform.interceptor.ts
│   │   │
│   │   └── database/                 # DB 설정
│   │       ├── database.module.ts
│   │       └── migrations/
│   │
│   ├── test/
│   │   ├── e2e/
│   │   └── unit/
│   │
│   ├── .env.development
│   ├── .env.production
│   ├── nest-cli.json
│   ├── package.json
│   └── tsconfig.json
│
├── 📂 shared-packages/               # 공유 패키지
│   └── be-shared-packages/
│       ├── packages/
│       │   ├── types/                # @geobuk/shared-types
│       │   │   └── src/
│       │   │       ├── api/
│       │   │       ├── member/
│       │   │       ├── planner/
│       │   │       └── common/
│       │   │
│       │   ├── entities/             # @geobuk/shared-entities
│       │   │   └── src/
│       │   │       ├── member/
│       │   │       ├── planner/
│       │   │       └── common/
│       │   │
│       │   └── utils/                # @geobuk/common-utils
│       │       └── src/
│       │           ├── jwt/
│       │           ├── bcrypt/
│       │           └── helpers/
│       │
│       └── docs/
│           ├── be-DATABASE-SCHEMA.md
│           └── be-INTEGRATION-GUIDE.md
│
├── 📂 docs/                          # 문서
│   ├── ARCHITECTURE.md               # 이 문서
│   ├── API.md                        # API 문서 (예정)
│   └── DEPLOYMENT.md                 # 배포 가이드 (예정)
│
├── docker-compose.yml                # 로컬 개발 환경 (예정)
├── .gitignore
└── README.md
```

---

## 4. 기술 스택

### 4.1 Frontend

| 기술 | 버전 | 용도 |
|------|------|------|
| React | 18.3 | UI 라이브러리 |
| Vite | 5.x | 빌드 도구 |
| TanStack Router | 1.45 | 라우팅 |
| TanStack Query | 5.x | 서버 상태 관리 |
| Tailwind CSS | 3.4 | 스타일링 |
| shadcn/ui | - | UI 컴포넌트 |
| Zustand | 4.x | 클라이언트 상태 관리 |
| Axios | 1.7 | HTTP 클라이언트 |

### 4.2 Backend

| 기술 | 버전 | 용도 |
|------|------|------|
| NestJS | 10.x | 백엔드 프레임워크 |
| TypeORM | 0.3.x | ORM |
| PostgreSQL | 14+ | 데이터베이스 |
| class-validator | 0.14 | DTO 검증 |
| class-transformer | 0.5 | 객체 변환 |
| Swagger | 7.x | API 문서화 |
| Passport | 0.7 | 인증 |

### 4.3 공유 패키지

| 패키지 | 설명 |
|--------|------|
| @geobuk/shared-types | 공유 타입 정의 (인터페이스, enum, DTO) |
| @geobuk/shared-entities | 공유 TypeORM 엔티티 |
| @geobuk/common-utils | 공통 유틸리티 (JWT, bcrypt, 헬퍼) |

---

## 5. API 설계

### 5.1 엔드포인트 목록

#### Score (성적 관리)

| Method | Endpoint | 설명 | 인증 |
|--------|----------|------|------|
| POST | `/api/scores/raw` | 원점수 저장 | ✅ |
| POST | `/api/scores/standard` | 표준점수 저장 | ✅ |
| GET | `/api/scores` | 전체 점수 목록 | ✅ |
| GET | `/api/scores/:examId` | 특정 시험 점수 조회 | ✅ |
| GET | `/api/scores/history` | 점수 히스토리 | ✅ |
| DELETE | `/api/scores/:id` | 점수 삭제 | ✅ |

#### Analysis (성적 분석)

| Method | Endpoint | 설명 | 인증 |
|--------|----------|------|------|
| GET | `/api/analysis/summary` | 성적 분석 요약 | ✅ |
| GET | `/api/analysis/combination` | 조합별 분석 | ✅ |
| GET | `/api/analysis/reflection` | 반영비율 분석 | ✅ |
| GET | `/api/analysis/achievement` | 성취수준 분석 | ✅ |

#### Prediction (대학 예측)

| Method | Endpoint | 설명 | 인증 |
|--------|----------|------|------|
| GET | `/api/prediction/universities` | 대학 예측 목록 | ✅ |
| GET | `/api/prediction/:universityId` | 특정 대학 상세 예측 | ✅ |
| GET | `/api/prediction/comparison` | 대학 비교 | ✅ |

#### University (대학 정보)

| Method | Endpoint | 설명 | 인증 |
|--------|----------|------|------|
| GET | `/api/universities` | 대학 목록 | ❌ |
| GET | `/api/universities/:id` | 대학 상세 | ❌ |
| GET | `/api/universities/search` | 대학 검색 | ❌ |
| GET | `/api/universities/:id/admissions` | 대학 전형 정보 | ❌ |

#### Statistics (누적 통계)

| Method | Endpoint | 설명 | 인증 |
|--------|----------|------|------|
| GET | `/api/statistics/cumulative` | 누적 분석 | ✅ |
| GET | `/api/statistics/trend` | 추이 분석 | ✅ |
| GET | `/api/statistics/rank` | 순위 분석 | ✅ |

### 5.2 응답 형식

```typescript
// 성공 응답
{
  "success": true,
  "data": { ... },
  "meta": {
    "timestamp": "2024-12-17T10:30:00.000Z"
  }
}

// 에러 응답
{
  "success": false,
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "점수는 0~100 사이여야 합니다.",
    "details": [ ... ]
  }
}

// 페이지네이션 응답
{
  "success": true,
  "data": [ ... ],
  "meta": {
    "page": 1,
    "limit": 10,
    "total": 100,
    "totalPages": 10
  }
}
```

---

## 6. 데이터베이스 설계

### 6.1 테이블 분류

#### 공용 테이블 (메인 DB에서 참조, 읽기 전용)

| 테이블명 | 설명 |
|---------|------|
| member_tb | 회원 정보 |
| ts_universities | 대학 정보 |
| ts_admissions | 학부 정보 |
| ts_regular_admissions | 정시 전형 |
| ts_regular_admission_prev_results | 정시 입결 |

#### 신규 테이블 (이 서비스 전용)

| 테이블명 | 설명 |
|---------|------|
| mockexam_raw_score_tb | 모의고사 원점수 |
| mockexam_standard_score_tb | 모의고사 표준점수 |
| mockexam_analysis_cache_tb | 분석 결과 캐시 |
| member_target_university_tb | 목표 대학 설정 |

### 6.2 ERD

```
┌─────────────────────────────────────────────────────────────────┐
│                       member_tb (공용)                          │
│                           │                                     │
│         ┌─────────────────┼─────────────────┐                  │
│         │                 │                 │                  │
│         ▼                 ▼                 ▼                  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │ raw_score_tb │  │ std_score_tb │  │ target_univ  │         │
│  │              │  │              │  │              │         │
│  │ - member_id  │  │ - member_id  │  │ - member_id  │         │
│  │ - exam_year  │  │ - exam_year  │  │ - univ_id    │         │
│  │ - exam_month │  │ - exam_month │  │ - priority   │         │
│  │ - korean     │  │ - korean     │  └──────────────┘         │
│  │ - math       │  │ - math       │           │               │
│  │ - english    │  │ - english    │           ▼               │
│  │ - ...        │  │ - ...        │  ┌──────────────┐         │
│  └──────────────┘  └──────────────┘  │ ts_univ (공용)│         │
│                                      └──────────────┘         │
└─────────────────────────────────────────────────────────────────┘
```

### 6.3 주요 엔티티

```typescript
// mockexam_raw_score_tb
@Entity('mockexam_raw_score_tb')
export class MockExamRawScoreEntity {
  @PrimaryGeneratedColumn({ type: 'bigint' })
  id: number;

  @Column({ name: 'member_id', type: 'bigint' })
  memberId: number;

  @Column({ name: 'exam_year', type: 'int' })
  examYear: number;

  @Column({ name: 'exam_month', type: 'int' })
  examMonth: number;

  @Column({ name: 'grade_level', type: 'int' })
  gradeLevel: number; // 1, 2, 3 (고1, 고2, 고3)

  @Column({ name: 'korean_type', type: 'varchar', length: 10, nullable: true })
  koreanType: string; // 화작, 언매

  @Column({ name: 'korean_score', type: 'int', nullable: true })
  koreanScore: number;

  @Column({ name: 'math_type', type: 'varchar', length: 10, nullable: true })
  mathType: string; // 확통, 미적, 기하

  @Column({ name: 'math_score', type: 'int', nullable: true })
  mathScore: number;

  @Column({ name: 'english_score', type: 'int', nullable: true })
  englishScore: number;

  // ... 기타 과목

  @CreateDateColumn({ name: 'created_at' })
  createdAt: Date;

  @UpdateDateColumn({ name: 'updated_at' })
  updatedAt: Date;
}
```

---

## 7. 인증 및 보안

### 7.1 JWT 인증

메인 백엔드(GB-Back-Nest)와 **동일한 JWT 시크릿**을 사용하여 토큰을 검증합니다.

```typescript
// JWT 검증 흐름
[Frontend] → Bearer Token → [Backend] → JWT 검증 → [요청 처리]
                                │
                                └─→ @geobuk/common-utils의 JwtService 사용
```

### 7.2 환경 변수

```env
# .env.development
NODE_ENV=development
PORT=4003

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=tsuser
DB_PASSWORD=tsuser1234
DB_DATABASE=geobukschool_dev

# JWT (메인 백엔드와 동일)
AUTH_JWT_SECRET=<메인백엔드와 동일한 시크릿>
AUTH_REFRESH_SECRET=<메인백엔드와 동일한 시크릿>
```

### 7.3 보안 고려사항

- ⚠️ `member_tb`는 **읽기 전용**으로만 사용
- ⚠️ 회원 정보 수정은 메인 백엔드에서만 수행
- ✅ 모든 API는 rate limiting 적용
- ✅ SQL Injection 방지 (TypeORM 파라미터 바인딩)
- ✅ XSS 방지 (class-validator 입력 검증)

---

## 8. 개발 환경 설정

### 8.1 필수 요구사항

- Node.js 18+
- PostgreSQL 14+
- npm 또는 yarn

### 8.2 로컬 개발 환경 구성

```bash
# 1. 프로젝트 클론
git clone <repository-url>
cd MyExam

# 2. 프론트엔드 설정
cd frontend
npm install
npm run dev  # http://localhost:3000

# 3. 백엔드 설정 (예정)
cd ../backend
npm install
npm run start:dev  # http://localhost:4003

# 4. 공유 패키지 빌드 (필요시)
cd ../shared-packages/be-shared-packages
yarn install
yarn build
```

### 8.3 Docker Compose (예정)

```yaml
# docker-compose.yml
version: '3.8'
services:
  postgres:
    image: postgres:14
    environment:
      POSTGRES_DB: geobukschool_dev
      POSTGRES_USER: tsuser
      POSTGRES_PASSWORD: tsuser1234
    ports:
      - "5432:5432"

  backend:
    build: ./backend
    ports:
      - "4003:4003"
    depends_on:
      - postgres

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
```

---

## 부록

### A. 명명 규칙

| 대상 | 규칙 | 예시 |
|------|------|------|
| 파일명 | kebab-case | `score.service.ts` |
| 클래스 | PascalCase | `ScoreService` |
| 변수/함수 | camelCase | `getScoreById` |
| DB 테이블 | snake_case + _tb | `mockexam_raw_score_tb` |
| DB 컬럼 | snake_case | `member_id`, `created_at` |
| API 경로 | kebab-case | `/api/score-analysis` |

### B. Git 커밋 컨벤션

```
<type>(<scope>): <subject>

# 예시
feat(score): 원점수 입력 API 추가
fix(analysis): 조합별 분석 계산 오류 수정
docs(readme): 설치 가이드 추가
```

### C. 관련 문서

- [데이터베이스 스키마](../shared-packages/be-shared-packages/docs/be-DATABASE-SCHEMA.md)
- [통합 가이드](../shared-packages/be-shared-packages/docs/be-INTEGRATION-GUIDE.md)
- [서비스 설정 가이드](../shared-packages/be-shared-packages/be-SETUP-SERVICE.md)

---

*이 문서는 프로젝트 진행에 따라 업데이트됩니다.*














