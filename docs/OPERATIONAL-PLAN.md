# 모의고사 분석 시스템 - 작동 계획서

> **버전**: 1.0.0  
> **작성일**: 2024-12-17  
> **상태**: 계획 단계

---

## 📋 목차

1. [시스템 개요](#1-시스템-개요)
2. [기능별 API 매핑](#2-기능별-api-매핑)
3. [데이터 흐름](#3-데이터-흐름)
4. [개발 우선순위](#4-개발-우선순위)
5. [개발 로드맵](#5-개발-로드맵)

---

## 1. 시스템 개요

### 1.1 핵심 기능

| # | 기능 | 프론트엔드 페이지 | 설명 |
|---|------|------------------|------|
| 1 | 모의고사 입력 | `/mock-analysis/input` | 모의고사 선택 (연도/학년/월) |
| 2 | 점수 입력 | `/mock-analysis/input/score` | 원점수/표준점수 입력 (학년별 다른 폼) |
| 3 | 정답 입력 | `/mock-analysis/input/form` | 문항별 정답 직접 입력 |
| 4 | 성적 분석 | `/mock-analysis/score-analysis` | 과목별/조합별/반영비율별 분석 |
| 5 | 대학 예측 | `/mock-analysis/prediction` | 대학 합격 가능성 예측 |
| 6 | 누적 분석 | `/mock-analysis/statistics` | 여러 회차 성적 추이 분석 |
| 7 | 결과 관리 | `/mock-analysis/management` | 저장된 분석 결과/오답 관리 |
| 8 | 목표 대학 | `/mock-analysis/target-university` | 목표 대학 설정 및 비교 |
| 9 | 오답 노트 | `/mock-analysis/wrong-answers` | 틀린 문제 분석 |

### 1.2 데이터베이스 모델 (15개)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            핵심 엔티티 관계                                   │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│    ┌──────────┐         ┌───────────────┐         ┌─────────────────┐       │
│    │ Student  │ ───1:N──│ StudentScore  │──N:1────│   MockExam      │       │
│    └──────────┘         └───────────────┘         └────────┬────────┘       │
│         │                                                   │               │
│         │ 1:N                                               │ 1:N           │
│         ▼                                                   ▼               │
│    ┌──────────────┐                               ┌─────────────────┐       │
│    │StudentTarget │                               │  ExamQuestion   │       │
│    └──────────────┘                               └─────────────────┘       │
│         │                                                                   │
│         │ N:1                                                               │
│         ▼                                                                   │
│    ┌───────────────┐     ┌──────────────────┐     ┌─────────────────┐       │
│    │  Department   │──N:1│   University     │     │ AdmissionCutoff │       │
│    └───────────────┘     └──────────────────┘     └─────────────────┘       │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 2. 기능별 API 매핑

### 2.1 모의고사 입력 (Input)

#### 프론트엔드 요구사항
- 연도/학년/월 선택 → 해당 모의고사 존재 여부 확인
- 점수입력/정답입력 분기

#### API 설계

| Method | Endpoint | 설명 | 관련 테이블 |
|--------|----------|------|-------------|
| GET | `/api/mock-exams` | 모의고사 목록 조회 | `mock_exams` |
| GET | `/api/mock-exams/search` | 연도/학년/월로 검색 | `mock_exams` |
| GET | `/api/mock-exams/:code` | 특정 모의고사 상세 | `mock_exams` |

#### 데이터 흐름
```
[사용자] ─선택→ [Frontend]
          ↓
    GET /api/mock-exams/search?year=2024&grade=H3&month=3
          ↓
    [Backend] ─조회→ MockExam.findOne({ code: 'H32403' })
          ↓
    { id, code, name, grade, year, month, type }
```

---

### 2.2 점수 입력 (Score Input)

#### 프론트엔드 요구사항
- **고1**: 국어, 수학, 영어, 한국사, 통합과학, 통합사회 (원점수/표준점수)
- **고2**: 국어, 수학, 영어, 한국사, 탐구1, 탐구2 (원점수/표준점수+등급+백분위)
- **고3**: 국어(선택과목), 수학(선택과목), 영어, 한국사, 탐구1, 탐구2, 제2외국어 (표준점수+등급+백분위)

#### API 설계

| Method | Endpoint | 설명 | 관련 테이블 |
|--------|----------|------|-------------|
| POST | `/api/scores` | 점수 저장 | `student_scores` |
| PUT | `/api/scores/:id` | 점수 수정 | `student_scores` |
| GET | `/api/scores/:studentId/:mockExamId` | 특정 점수 조회 | `student_scores` |
| GET | `/api/subject-codes` | 과목 코드 목록 | `subject_codes` |
| GET | `/api/subject-areas` | 교과 영역 목록 | `subject_areas` |

#### DTO 설계

```typescript
// 고3 표준점수 입력 DTO
interface CreateGrade3StandardScoreDto {
  studentId: number;
  mockExamId: number;
  
  // 국어
  koreanSelection: string;      // '화작' | '언매'
  koreanStandard: number;       // 표준점수
  koreanGrade: number;          // 등급 (1-9)
  koreanPercentile: number;     // 백분위
  
  // 수학
  mathSelection: string;        // '확통' | '미적' | '기하'
  mathStandard: number;
  mathGrade: number;
  mathPercentile: number;
  
  // 영어 (등급제)
  englishGrade: number;
  
  // 한국사 (등급제)
  historyGrade: number;
  
  // 탐구
  inquiry1Selection: string;    // 과목명
  inquiry1Standard: number;
  inquiry1Grade: number;
  inquiry1Percentile: number;
  
  inquiry2Selection: string;
  inquiry2Standard: number;
  inquiry2Grade: number;
  inquiry2Percentile: number;
  
  // 제2외국어 (선택)
  foreignSelection?: string;
  foreignGrade?: number;
}
```

#### 데이터 흐름
```
[사용자] ─점수입력→ [Frontend: ScoreInputPage]
          ↓
    POST /api/scores
    Body: { studentId, mockExamId, koreanStandard, ... }
          ↓
    [Backend: ScoreService]
      1. 학생 존재 확인 (Student.findById)
      2. 모의고사 존재 확인 (MockExam.findById)
      3. 기존 점수 확인 (upsert 처리)
      4. StudentScore.create/update
          ↓
    [Response] { success: true, data: { id, ... } }
```

---

### 2.3 성적 분석 (Score Analysis)

#### 프론트엔드 요구사항
- **내 성적 표**: 과목별 표준점수/백분위/등급/상대등급
- **반영비율 분석**: 대학별 반영비율에 따른 점수 차이
- **조합별 분석**: 국수탐, 국영탐 등 조합별 성적
- **성취수준 차트**: 막대 그래프 시각화

#### API 설계

| Method | Endpoint | 설명 | 관련 테이블 |
|--------|----------|------|-------------|
| GET | `/api/analysis/summary/:scoreId` | 성적 분석 요약 | `student_scores` |
| GET | `/api/analysis/reflection` | 반영비율 분석 | `departments`, `student_scores` |
| GET | `/api/analysis/combination` | 조합별 분석 | `student_scores` |
| GET | `/api/analysis/achievement` | 성취수준 분석 | `student_scores`, `score_conversion_*` |

#### 분석 로직

```typescript
// 반영비율 분석 서비스
interface ReflectionAnalysisService {
  analyze(scoreId: number): Promise<ReflectionAnalysisResult> {
    // 1. 학생 점수 조회
    const score = await StudentScore.findById(scoreId);
    
    // 2. 샘플 대학 반영비율 조회 (A, B, C, D 대학)
    const universities = await Department.findSampleForAnalysis();
    
    // 3. 각 대학 반영비율로 환산점수 계산
    const results = universities.map(univ => ({
      name: univ.university.name,
      koreanRatio: parseFloat(univ.koreanRatio),
      mathRatio: parseFloat(univ.mathRatio),
      inquiryRatio: parseFloat(univ.inquiryRatio),
      calculatedScore: this.calculateScore(score, univ),
      grade: this.calculateGrade(...)
    }));
    
    return { scores: score, analysis: results };
  }
}
```

---

### 2.4 대학 예측 (University Prediction)

#### 프론트엔드 요구사항
- 지역 필터 (서울, 경기, 인천 등 15개)
- 계열 필터 (자연, 인문, 예체능, 융합)
- 대학별 점수 범위 차트 (내 점수와 비교)
- 대학 선택 및 비교 기능

#### API 설계

| Method | Endpoint | 설명 | 관련 테이블 |
|--------|----------|------|-------------|
| GET | `/api/universities` | 대학 목록 | `universities` |
| GET | `/api/universities/filter` | 지역/계열 필터링 | `universities`, `departments` |
| GET | `/api/prediction/:studentId/:mockExamId` | 대학 예측 | 복합 쿼리 |
| GET | `/api/admission-cutoffs/:departmentId` | 입결 조회 | `admission_cutoffs` |

#### 예측 알고리즘

```typescript
interface PredictionService {
  async predict(studentId: number, mockExamId: number, filters: PredictionFilter) {
    // 1. 학생 점수 조회
    const score = await StudentScore.findOne({ studentId, mockExamId });
    
    // 2. 필터링된 학과 조회
    const departments = await Department.findByFilters({
      region: filters.region,
      category: filters.category,
    });
    
    // 3. 각 학과별 환산점수 계산 (1000점 만점 통일)
    const predictions = await Promise.all(
      departments.map(async (dept) => {
        // 대학별 계산식 적용
        const convertedScore = this.convertTo1000(score, dept);
        
        // 입결 데이터 조회
        const cutoff = await AdmissionCutoff.findLatest(dept.id);
        
        return {
          university: dept.university,
          department: dept,
          myScore: convertedScore,
          cutoffMin: cutoff?.firstCutScore,
          cutoffMax: cutoff?.finalCutScore,
          riskLevel: this.calculateRisk(convertedScore, cutoff),
        };
      })
    );
    
    return predictions.sort((a, b) => b.myScore - a.myScore);
  }
}
```

---

### 2.5 누적 분석 (Statistics)

#### 프론트엔드 요구사항
- 전체 과목 / 과목별 뷰 전환
- 그래프 종류: 등급, 백분위, 표준점수, 원점수
- 모의고사별 성적 추이 라인 차트

#### API 설계

| Method | Endpoint | 설명 | 관련 테이블 |
|--------|----------|------|-------------|
| GET | `/api/statistics/trend/:studentId` | 성적 추이 | `student_scores`, `mock_exams` |
| GET | `/api/statistics/cumulative/:studentId` | 누적 분석 | `student_scores` |
| GET | `/api/statistics/by-subject/:studentId` | 과목별 분석 | `student_scores` |

#### 통계 로직

```typescript
interface StatisticsService {
  async getTrend(studentId: number, subject?: string) {
    // 1. 학생의 모든 점수 조회 (시간순 정렬)
    const scores = await StudentScore.findAll({
      where: { studentId },
      include: [MockExam],
      order: [['mockExam', 'year', 'ASC'], ['mockExam', 'month', 'ASC']],
    });
    
    // 2. 차트 데이터 변환
    return scores.map(score => ({
      examName: `${score.mockExam.year}년 ${score.mockExam.month}월`,
      grade: subject ? score[`${subject}Grade`] : this.avgGrade(score),
      percentile: subject ? score[`${subject}Percentile`] : this.avgPercentile(score),
      standardScore: subject ? score[`${subject}Standard`] : this.sumStandard(score),
    }));
  }
}
```

---

### 2.6 목표 대학 (Target University)

#### 프론트엔드 요구사항
- 목표 대학 최대 5개 설정
- 모의고사별 내 등급 vs 대학 등급컷 라인 차트

#### API 설계

| Method | Endpoint | 설명 | 관련 테이블 |
|--------|----------|------|-------------|
| GET | `/api/targets/:studentId` | 목표 대학 조회 | `student_targets` |
| POST | `/api/targets` | 목표 대학 추가 | `student_targets` |
| DELETE | `/api/targets/:id` | 목표 대학 삭제 | `student_targets` |
| GET | `/api/targets/:studentId/comparison` | 등급 비교 | 복합 쿼리 |

---

### 2.7 오답 노트 (Wrong Answers)

#### 프론트엔드 요구사항
- 틀린 문제 현황 (전체/과목별)
- 문항별 정답/오답 표시
- 오답 유형 분석

#### API 설계

| Method | Endpoint | 설명 | 관련 테이블 |
|--------|----------|------|-------------|
| GET | `/api/wrong-answers/:studentId/:mockExamId` | 오답 조회 | 별도 테이블 필요 |
| GET | `/api/exam-questions/:mockExamId` | 문제 정보 | `exam_questions` |
| POST | `/api/student-answers` | 학생 답안 저장 | 별도 테이블 필요 |

#### 추가 테이블 필요

```prisma
// 학생 답안 (신규)
model StudentAnswer {
  id              Int      @id @default(autoincrement())
  studentId       Int      @map("student_id")
  examQuestionId  Int      @map("exam_question_id")
  selectedAnswer  Int      @map("selected_answer")  // 학생이 선택한 답
  isCorrect       Boolean  @map("is_correct")
  
  student         Student      @relation(...)
  examQuestion    ExamQuestion @relation(...)
}
```

---

## 3. 데이터 흐름

### 3.1 점수 입력 → 분석 전체 흐름

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            점수 입력 → 분석 흐름                              │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  [1. 모의고사 선택]                                                          │
│       │                                                                     │
│       ▼                                                                     │
│  ┌────────────────────┐                                                     │
│  │  GET /mock-exams   │ → MockExam { code: 'H32403', name: '고3 24년 3월' } │
│  └────────────────────┘                                                     │
│       │                                                                     │
│       ▼                                                                     │
│  [2. 점수 입력]                                                              │
│       │                                                                     │
│       ▼                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐          │
│  │  POST /scores                                                 │          │
│  │  Body: { studentId, mockExamId, koreanStandard: 130, ... }   │          │
│  └──────────────────────────────────────────────────────────────┘          │
│       │                                                                     │
│       │ 자동 계산                                                            │
│       ▼                                                                     │
│  ┌──────────────────────────────────────────────────────────────┐          │
│  │  StudentScore 저장                                            │          │
│  │  - 국수탐 표준점수 합계 자동 계산                               │          │
│  │  - 상위누백 자동 계산 (ScoreConversionStandard 참조)           │          │
│  └──────────────────────────────────────────────────────────────┘          │
│       │                                                                     │
│       ▼                                                                     │
│  [3. 성적 분석]                                                              │
│       │                                                                     │
│       ├──────────────────────────────────────┐                             │
│       ▼                                      ▼                             │
│  ┌─────────────────┐              ┌─────────────────────┐                  │
│  │ 반영비율 분석    │              │    대학 예측         │                  │
│  │ (대학별 환산)    │              │ (1000점 통일 비교)   │                  │
│  └─────────────────┘              └─────────────────────┘                  │
│       │                                      │                             │
│       │                                      ▼                             │
│       │                           ┌─────────────────────┐                  │
│       │                           │  AdmissionCutoff    │                  │
│       │                           │  (입결 데이터 참조)  │                  │
│       │                           └─────────────────────┘                  │
│       │                                                                     │
│       ▼                                                                     │
│  [4. 결과 저장 & 시각화]                                                     │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 대학 예측 계산 흐름

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            대학 예측 계산 흐름                                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  [학생 점수]                                                                 │
│  ┌─────────────────────────────────────────┐                                │
│  │ 국어: 130 (표점), 89 (백분위), 2 (등급)  │                                │
│  │ 수학: 135 (표점), 92 (백분위), 2 (등급)  │                                │
│  │ 영어: 2 (등급)                           │                                │
│  │ 탐구: 65+68 (표점), 90+88 (백분위)       │                                │
│  └─────────────────────────────────────────┘                                │
│       │                                                                     │
│       ▼                                                                     │
│  [대학별 환산]                                                               │
│  ┌─────────────────────────────────────────────────────────────────┐        │
│  │ 서울대 (자연계열)                                                │        │
│  │ - 반영비율: 국어 33.3%, 수학 40%, 탐구 26.7%                     │        │
│  │ - 수능요소: 표점 + 변표                                          │        │
│  │ - 영어: 등급별 감점 (2등급 = -0.5)                               │        │
│  │ - 한국사: 등급별 가산점                                          │        │
│  │ - 가산점: 미적/기하 +3%, 과탐 +2%                                │        │
│  │                                                                  │        │
│  │ 환산 = 130*0.333 + 135*0.4 + (65+68)/2*0.267 + 가산점 - 감점    │        │
│  │ → 1000점 환산 = 환산점수 * 환산율                                │        │
│  └─────────────────────────────────────────────────────────────────┘        │
│       │                                                                     │
│       ▼                                                                     │
│  [입결 비교]                                                                 │
│  ┌─────────────────────────────────────────────────────────────────┐        │
│  │ 서울대 컴퓨터공학 (2024 입결)                                    │        │
│  │ - 최초컷: 295.5 (1000점 환산: 852)                               │        │
│  │ - 추합컷: 293.2 (1000점 환산: 846)                               │        │
│  │                                                                  │        │
│  │ 내 점수 (1000점 환산): 842                                       │        │
│  │ → 위험도: -1 (추합 가능)                                         │        │
│  └─────────────────────────────────────────────────────────────────┘        │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 4. 개발 우선순위

### 4.1 Phase 1: 핵심 기능 (MVP)

| 순서 | 기능 | 프론트엔드 | 백엔드 | 우선순위 |
|------|------|-----------|--------|----------|
| 1 | 모의고사 조회 | ✅ 완료 | 🔲 개발 필요 | 🔴 최우선 |
| 2 | 점수 입력 (고3) | ✅ 완료 | 🔲 개발 필요 | 🔴 최우선 |
| 3 | 성적 분석 (기본) | ✅ 완료 | 🔲 개발 필요 | 🔴 최우선 |
| 4 | 대학 목록 조회 | ✅ 완료 | 🔲 개발 필요 | 🟡 중요 |

### 4.2 Phase 2: 확장 기능

| 순서 | 기능 | 프론트엔드 | 백엔드 | 우선순위 |
|------|------|-----------|--------|----------|
| 5 | 대학 예측 | ✅ 완료 | 🔲 개발 필요 | 🟡 중요 |
| 6 | 점수 입력 (고1/고2) | ✅ 완료 | 🔲 개발 필요 | 🟡 중요 |
| 7 | 누적 분석 | ✅ 완료 | 🔲 개발 필요 | 🟢 보통 |
| 8 | 목표 대학 | ✅ 완료 | 🔲 개발 필요 | 🟢 보통 |

### 4.3 Phase 3: 고급 기능

| 순서 | 기능 | 프론트엔드 | 백엔드 | 우선순위 |
|------|------|-----------|--------|----------|
| 9 | 정답 입력 | ✅ 완료 | 🔲 개발 필요 | ⚪ 낮음 |
| 10 | 오답 노트 | ✅ 완료 | 🔲 개발 필요 | ⚪ 낮음 |
| 11 | 결과 관리 | ✅ 완료 | 🔲 개발 필요 | ⚪ 낮음 |

---

## 5. 개발 로드맵

### 5.1 Sprint 1 (2주): 기반 구축

```
Week 1:
├── Backend 기본 구조 설정
│   ├── NestJS 모듈 구조 생성
│   ├── Prisma Client 연동
│   └── 기본 CRUD 서비스
│
├── API 개발
│   ├── GET /api/mock-exams
│   ├── GET /api/subject-codes
│   └── GET /api/subject-areas
│
└── 프론트엔드-백엔드 연동
    └── 모의고사 선택 페이지 API 연결

Week 2:
├── 점수 입력 API
│   ├── POST /api/scores (고3 표준점수)
│   ├── PUT /api/scores/:id
│   └── GET /api/scores/:studentId
│
└── 자동 계산 로직
    ├── 표준점수 합계 계산
    └── 상위누백 계산
```

### 5.2 Sprint 2 (2주): 분석 기능

```
Week 3:
├── 성적 분석 API
│   ├── GET /api/analysis/summary
│   └── GET /api/analysis/combination
│
└── 반영비율 분석
    └── 대학별 환산 로직 구현

Week 4:
├── 대학 예측 API
│   ├── GET /api/universities
│   ├── GET /api/prediction
│   └── 1000점 통일 환산 로직
│
└── 프론트엔드 연동
    ├── 성적 분석 페이지 API 연결
    └── 대학 예측 페이지 API 연결
```

### 5.3 Sprint 3 (2주): 확장 기능

```
Week 5:
├── 점수 입력 확장
│   ├── 고1/고2 점수 입력 API
│   └── 학년별 분기 처리
│
└── 누적 분석 API
    └── 성적 추이 데이터 생성

Week 6:
├── 목표 대학 API
│   ├── CRUD 기능
│   └── 등급 비교 로직
│
└── 프론트엔드 마무리
    └── 전체 페이지 API 연동
```

### 5.4 Sprint 4 (2주): 고급 기능 & QA

```
Week 7:
├── 정답 입력 / 오답 노트
│   ├── 학생 답안 테이블 추가
│   └── 오답 분석 로직
│
└── 결과 관리 기능

Week 8:
├── 테스트 & 버그 수정
├── 성능 최적화
└── 배포 준비
```

---

## 부록: 구현 체크리스트

### Backend API 체크리스트

- [ ] `MockExamModule`
  - [ ] `GET /api/mock-exams`
  - [ ] `GET /api/mock-exams/search`
  - [ ] `GET /api/mock-exams/:code`

- [ ] `ScoreModule`
  - [ ] `POST /api/scores`
  - [ ] `PUT /api/scores/:id`
  - [ ] `GET /api/scores/:studentId`
  - [ ] `GET /api/scores/:studentId/:mockExamId`
  - [ ] `DELETE /api/scores/:id`

- [ ] `AnalysisModule`
  - [ ] `GET /api/analysis/summary/:scoreId`
  - [ ] `GET /api/analysis/reflection`
  - [ ] `GET /api/analysis/combination`
  - [ ] `GET /api/analysis/achievement`

- [ ] `PredictionModule`
  - [ ] `GET /api/universities`
  - [ ] `GET /api/universities/filter`
  - [ ] `GET /api/prediction/:studentId/:mockExamId`
  - [ ] `GET /api/admission-cutoffs/:departmentId`

- [ ] `StatisticsModule`
  - [ ] `GET /api/statistics/trend/:studentId`
  - [ ] `GET /api/statistics/cumulative/:studentId`
  - [ ] `GET /api/statistics/by-subject/:studentId`

- [ ] `TargetModule`
  - [ ] `GET /api/targets/:studentId`
  - [ ] `POST /api/targets`
  - [ ] `DELETE /api/targets/:id`
  - [ ] `GET /api/targets/:studentId/comparison`

- [ ] `WrongAnswerModule`
  - [ ] `GET /api/wrong-answers/:studentId/:mockExamId`
  - [ ] `GET /api/exam-questions/:mockExamId`
  - [ ] `POST /api/student-answers`

---

*이 문서는 프로젝트 진행에 따라 업데이트됩니다.*








